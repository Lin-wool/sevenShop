# 购物车数量输入优化规范

## 新增需求

### 需求:购物车必须支持直接输入数量
系统必须允许用户直接在输入框中修改商品数量，而不仅仅使用加减按钮。

#### 场景:用户直接输入数量
- **当** 用户在数量输入框中直接输入数字
- **那么** 系统必须接受该输入并更新购物车中的商品数量

#### 场景:用户输入无效数量
- **当** 用户输入非数字或负数时
- **那么** 系统必须自动修正为有效值（最小为1，最大为99）

#### 场景:数量变化后更新小计
- **当** 用户修改商品数量后
- **那么** 系统必须立即更新该商品的小计价格
