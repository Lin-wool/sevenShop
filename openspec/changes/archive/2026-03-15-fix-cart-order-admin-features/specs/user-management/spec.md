# 用户管理功能规范

## 新增需求

### 需求:管理员可以查看用户列表
系统必须允许管理员查看所有用户列表。

#### 场景:查看用户列表
- **当** 管理员访问用户管理页面
- **那么** 系统必须显示用户列表，包含用户名、昵称、状态等信息

### 需求:管理员可以禁用/启用用户
系统必须允许管理员禁用或启用用户账号。

#### 场景:禁用用户
- **当** 管理员点击禁用按钮
- **那么** 系统必须将用户状态标记为禁用

#### 场景:启用用户
- **当** 管理员点击启用按钮
- **那么** 系统必须将用户状态标记为正常
